####################################################################################################################
# WARNING: Do not store sensitive information in this file, as its contents will be included in the Qodana report. #
####################################################################################################################

# Qodana analyses one language per configuration file, so a hybrid repository
# needs one config per linter. This is the Rust half; qodana.yaml is Python.
#
# Two caveats, both from JetBrains' own documentation:
#   - qodana-rust is Early Access. JetBrains state it "may not be reliable,
#     may not work as intended, and may contain errors".
#   - It requires an Ultimate or Ultimate Plus licence, as qodana-python does.
#     If the Qodana Cloud project behind QODANA_TOKEN_983296264 is not on one
#     of those, this leg will fail and should be dropped.
#
# Static analysis only: this does not build or test the crate. The cargo
# gate in .github/workflows/ci.yml is what actually runs the test suite.
version: "1.0"
linter: qodana-rust
profile:
  name: qodana.recommended
include:
  - name: CheckDependencyLicenses
